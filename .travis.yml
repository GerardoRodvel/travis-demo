language: node_js
node_js:
  - "10"
dist: trusty
sudo: required

branches:
  only:
  - master

before_script:
  - npm install -g @angular/cli

script:
- ng lint
- npm run test-headless
- ng build --prod --base-href https://GerardoRodvel.github.io/travis-demo/ 

deploy:
   provider: pages
   skip_cleanup: true
   github_token: $3687093612e8a680ea16898046e24a7a782da959
   local_dir: dist/travis-demo
   on:
    branch: master